# 2026-09-08

## Discussed & executed: project rename, miuix base, branch refactor
- Rename: kept applicationId/namespace (`io.github.3togo.scrcaster`) non-breaking; set `rootProject.name` = `ScrCaster`; fixed `Miuzarte`→`3togo` refs in README + star-history. No new brand name chosen yet.
- miuix: added `branch = main` to `.gitmodules`; bumped submodule pin from `7cc339c` to `91301e8` (origin/main, +6 commits). Low-risk delta.
- Branches: adopted Git Flow. Created `develop` (from origin/main), renamed `codex/linux-desktop-support` → `feature/linux-desktop-support`, pushed both, deleted remote `codex/linux-desktop-support`.
- Changes left UNCOMMITTED in working tree (`.gitmodules`, `submodule/miuix` staged; `README.md`, `settings.gradle.kts` modified). Build not yet verified against new miuix pin.

## Renamed project to "ScrCaster"
- Chosen name "ScrCaster" (option A) after user rejected plain "Scrcpy" to avoid confusion with Genymobile/scrcpy.
- Platform-neutral (Android app + Linux desktop). Swept across README title, app_name (en+zh), AboutScreen.kt, rootProject.name, workspace file.
- Also updated desktop app display name "Scrcpy for Linux" → "ScrCaster" in DesktopApp.java (window title + error msg), GuiSmokeTest.java, desktop/package-deb.sh (Name=).
- Intentionally left: repo-name URLs (3togo/ScrCaster), Kotlin class/identifiers (ScrcpyOptions, Scrcpy.Session), and on-device storage dir "Scrcpy" (changing breaks existing user files).
- Reason: project also has a Linux desktop frontend, so "ScrCaster" was misleading.
- Display name changes: `settings.gradle.kts` rootProject.name `ScrCaster`→`Scrcpy`; README title; `app_name` strings (en+zh); `AboutScreen.kt`; renamed `ScrCaster.code-workspace`→`Scrcpy.code-workspace`.
- KEEP unchanged (non-breaking): `applicationId`/`namespace` = `io.github.3togo.scrcaster` (156 Kotlin files). GitHub repo name `3togo/ScrCaster` left as-is (manual rename).
